apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: pinit
spec:
  selector:
    app: pinit-notification
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
  